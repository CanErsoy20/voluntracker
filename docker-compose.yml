version: "3.9"
   
services:
  db:
    image: postgres
    ports:
      - "92.205.189.130:5432:5432"
    volumes:
      - ./database_folder/database_volume:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    networks:
      - container_network
    env_file:
      - db.env
  backend:
    image: afet_takip_backend
    build: ./backend
    volumes:
      - ./backend:/backend
      - ./uploads:/uploads
    ports:
      - "92.205.189.130:8080:8080"
    depends_on:
      - db
    networks:
      - container_network
networks:
  container_network:
    driver: bridge